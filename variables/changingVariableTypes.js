// just because a variable has 1 type to start with,
// that doesn't mean it always needs to store
// something OF THAT TYPE only (in JavaScript)
let someVariable = "some variable goes here";
console.log(someVariable);

// change the variable
someVariable = 732;
console.log(someVariable);
